##' Extract standardized mean differences from a (svy)TableOne object
##'
##' Extracts standardized mean differences data as a matrix from a (svy)TableOne object
##'
##'
##' @param x A stratified (svy)TableOne object containing standardized mean differences.
##' @return A matrix containing the average standardized mean differences as well as the all possible pairwise standardized mean differences. Variables are ordered in the same order as the printed table.
##' @author Kazuki Yoshida
##' @examples
##'
##' ##
##'
##' @export
ExtractSmd <- function(x) {
    NA
}
